import React from "react";
import AvatarUpload from "../components/AvatarUpload/AvatarUpload";

const Profile: React.FC = () => {
  return (
    <div>
      <AvatarUpload size={150} />
    </div>
  );
};

export default Profile;
